[package]
name = "math"
version = "0.1.0"
authors = ["ges0909 <gerrit.schrader@gmail.com>"]

[dependencies]
num = "*"

# Integration style tests seems to be possible only for lib
# crates and has to be in an own 'tests' dir.
[lib]
name = "math"
